model SystemDbConfig {
  id          String   @id @default(uuid())
  key         String   @unique
  value       String
  type   SystemConfigValueType
  description String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

enum SystemConfigValueType {
  NUMBER
  STRING
  BOOLEAN
}
